
  /* let numeroAscii = text.charCodeAt(i);
      console.log(numeroAscii);
      
      if (numeroAscii >=65 && numeroAscii <=90) {
        let cifradoMayus = (numeroAscii-65+number)%26+65;
        console.log(cifradoMayus);
        finalString += String.fromCharCode(cifradoMayus);
        console.log(finalString)
      } else if (numeroAscii >=97 && numeroAscii<=122) {
        let cifradoMinus = (numeroAscii-97+number)%26+97;
        finalString += String.fromCharCode(cifradoMinus);
        }
      }; return finalString;*/